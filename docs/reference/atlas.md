# Atlas API

`Atlas` is the central long-lived analysis object.

Use this page for exact signatures and docstring-level contracts.
For practical usage order, read `Start Here` and `Canonical Workflow` first.

::: cleo.atlas.Atlas
